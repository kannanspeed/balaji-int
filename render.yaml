services:
  - type: web
    name: balaji-interiors
    env: static
    buildCommand: echo "Static site - no build required"
    startCommand: echo "Static site deployed"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
